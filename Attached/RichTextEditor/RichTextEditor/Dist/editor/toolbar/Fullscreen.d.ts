/// <reference types="./types" />
import { MenuItem } from "./MenuItem";
export declare class Fullscreen extends MenuItem {
    constructor(vditor: LGEditor, menuItem: IMenuItem);
    _bindEvent(vditor: LGEditor, menuItem: IMenuItem): void;
}
